import PropertyListingCard from "@/components/properties/PropertyListingCard";

const PropertySearchCard = ({ property }) => (
  <PropertyListingCard property={property} variant="search" />
);

export default PropertySearchCard;
